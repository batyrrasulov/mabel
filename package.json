{
  "name": "mabel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @mabel/web",
    "build": "npm run build --workspace @mabel/web",
    "test": "npm run test --workspace @mabel/web",
    "typecheck": "npm run typecheck --workspace @mabel/web",
    "lint": "npm run lint --workspace @mabel/web"
  },
  "engines": {
    "node": ">=20"
  }
}
